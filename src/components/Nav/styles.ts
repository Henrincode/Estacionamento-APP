import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#66666610",
        padding: 10,
        position: "absolute",
        bottom: 0,
        width: "100%"
    }
})