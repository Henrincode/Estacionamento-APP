import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#666",
        padding: 10,
        position: "absolute",
        bottom: 0,
        width: "100%"
    },
    text: {
        color: "#f2f2f2"
    }
})